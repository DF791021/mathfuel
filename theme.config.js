/** @type {const} */
const themeColors = {
  // Primary blue from logo rocket
  primary: { light: '#2563EB', dark: '#3B82F6' },
  // Clean white background
  background: { light: '#FFFFFF', dark: '#0F172A' },
  // Light blue-gray surface
  surface: { light: '#F0F9FF', dark: '#1E293B' },
  // Dark text
  foreground: { light: '#1E293B', dark: '#F1F5F9' },
  // Muted blue-gray
  muted: { light: '#64748B', dark: '#94A3B8' },
  // Light border
  border: { light: '#E2E8F0', dark: '#334155' },
  // Green from logo (for success/correct)
  success: { light: '#22C55E', dark: '#4ADE80' },
  // Orange/gold from logo flames
  warning: { light: '#F59E0B', dark: '#FBBF24' },
  // Red for errors
  error: { light: '#EF4444', dark: '#F87171' },
  // Secondary green accent from logo
  accent: { light: '#16A34A', dark: '#22C55E' },
};

module.exports = { themeColors };
